export type PhotoSrc = string;
export type Link = {
    title: string,
    link: string,
    name: string,
  }
export type IndexData = {
    firstname: string;
    lastname: string;
    fullname: string;
    links: Link[];
}